export const focusClassIndicator = 'focusable-element-can-escape'

const focusClass = 'ring-4 ring-blue-400 outline-none'
const focusClassInset = 'ring-4 ring-blue-400 ring-inset outline-none'

export const focusRingClass = `${focusClassIndicator} ${focusClass}`
export const focusRingClassInset = `${focusClassIndicator} ${focusClassInset}`
